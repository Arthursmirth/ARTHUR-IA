const express = require('express');
const app = express();

const TELEGRAM_LINK = "https://t.me/druzzdev00"; 

app.get('/', (req, res) => {
    res.send(`
    <!DOCTYPE html>
    <html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>DEV MICHAEL - Officiel</title>
        <style>
            body { 
                background-color: #0f172a; 
                color: white; 
                font-family: sans-serif; 
                display: flex; 
                justify-content: center; 
                align-items: center; 
                height: 100vh; 
                margin: 0; 
            }
            .card { 
                background: #1e293b; 
                padding: 40px; 
                border-radius: 20px; 
                text-align: center; 
                border: 2px solid #00d2ff;
                box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            }
            h1 span { color: #00d2ff; }
            .btn { 
                background: #00d2ff; 
                color: #0f172a; 
                text-decoration: none; 
                padding: 15px 25px; 
                border-radius: 10px; 
                display: inline-block; 
                font-weight: bold;
                margin-top: 20px;
            }
        </style>
    </head>
    <body>
        <div class="card">
            <h1>DEV <span>MICHAEL</span></h1>
            <p>Système actif 24/7 sur Vercel.</p>
            <a href="${TELEGRAM_LINK}" class="btn">CONTACTER SUPPORT</a>
        </div>
    </body>
    </html>
    `);
});

module.exports = app;
