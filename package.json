{
  "name": "arthur-ia",
  "version": "1.0.0",
  "description": "Déploiement ARTHUR-IA sur Render",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}