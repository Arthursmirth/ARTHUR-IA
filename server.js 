const express = require('express');
const app = express();

const TELEGRAM_LINK = "https://t.me/druzzdev00"; 

app.get('/', (req, res) => {
    res.send(`
    <!DOCTYPE html>
    <html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>DEV MICHAEL - Officiel</title>
        <style>
            :root { --primary: #00d2ff; --secondary: #3a7bd5; --dark: #0f172a; }
            body { font-family: 'Segoe UI', sans-serif; background: var(--dark); color: white; margin: 0; display: flex; justify-content: center; align-items: center; height: 100vh; overflow: hidden; }
            .container { background: rgba(255, 255, 255, 0.05); backdrop-filter: blur(10px); padding: 40px; border-radius: 30px; border: 1px solid rgba(255, 255, 255, 0.1); text-align: center; max-width: 400px; width: 90%; box-shadow: 0 25px 50px rgba(0,0,0,0.5); }
            .dev-tag { background: linear-gradient(90deg, var(--primary), var(--secondary)); color: #000; padding: 5px 15px; border-radius: 50px; font-weight: bold; font-size: 0.8em; margin-bottom: 20px; display: inline-block; }
            h1 { margin: 0; font-size: 2.5em; letter-spacing: -1px; }
            h1 span { color: var(--primary); }
            p { color: #94a3b8; line-height: 1.6; margin: 20px 0; }
            .btn-tg { background: white; color: var(--dark); text-decoration: none; padding: 15px; border-radius: 15px; display: flex; align-items: center; justify-content: center; font-weight: bold; transition: all 0.3s; gap: 10px; }
            .btn-tg:hover { background: var(--primary); transform: translateY(-3px); }
            .status-dot { height: 8px; width: 8px; background-color: #22c55e; border-radius: 50%; display: inline-block; margin-right: 5px; box-shadow: 0 0 10px #22c55e; }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="dev-tag">DEVELOPPER</div>
            <h1>DEV <span>MICHAEL</span></h1>
            <p><span class="status-dot"></span> Système actif 24/7 sur Vercel. Tout est configuré pour fonctionner automatiquement.</p>
            
            <a href="${TELEGRAM_LINK}" class="btn-tg" target="_blank">
                💬 CONTACTER SUPPORT
            </a>
            
            <div style="margin-top: 30px; font-size: 0.7em; color: #475569;">
                &copy; 2026 MICHAEL DEV - ALL RIGHTS RESERVED
            </div>
        </div>
    </body>
    </html>
    `);
});

module.exports = app;
